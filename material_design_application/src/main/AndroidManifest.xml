<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zhangli.test.material_design_application">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GuideActivity" />
        <activity
            android:name=".MainActivity"
            android:label="实训">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".News_Details_Activity"
            android:label="新闻详情" />
        <activity android:name=".WallPaper_Activity" />
        <activity android:name=".Member_Activity" />
        <activity android:name=".MovieDetails_Activity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".WeatherActivity" />

        <!-- <service -->
        <!-- android:name=".AutoUpdateService" -->
        <!-- android:enabled="true" -->
        <!-- android:exported="true" /> -->
        <service
            android:name=".Service.AutoUpdateService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".SystemInfoActivity" />
        <activity android:name=".TestSrceen_Activity" />
        <activity android:name=".AppActivity" />
        <activity android:name=".SystemActivity" />
        <activity android:name=".ShareActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.VIEW" /> -->
            <!-- <category android:name="android.intent.category.DEFAULT" /> -->
            <!-- <category android:name="android.intent.category.BROWSABLE" /> -->
            <!-- &lt;!&ndash;<data android:scheme="tencent你的AppId" />&ndash;&gt; -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".MusicDetails_Activity"></activity>
    </application>

</manifest>